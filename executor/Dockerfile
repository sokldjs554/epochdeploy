FROM golang:1.23-alpine AS build
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go test ./... && CGO_ENABLED=0 go build -o /out/executor ./cmd/server

FROM gcr.io/distroless/static-debian12:nonroot
COPY --from=build /out/executor /executor
EXPOSE 9080
ENTRYPOINT ["/executor"]
